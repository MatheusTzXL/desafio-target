package Logica;

// 3) Lógica do código fornecido
public class Soma {
    public int calcularSoma(int limite) {
        int indice = 12, soma = 0, k = 1;

        while (k < indice) {
            k += 1;
            soma += k;
        }
        return soma;
    }
}
